# 项目上下文与接续指南 (Project Context & Handover)

> 本文档用于在新的 Antigravity 会话中**一秒无缝承接所有背景、架构、最新成果与后续待办任务**。在新项目中开启会话后，直接让智能体阅读本文档即可。

---

## 一、 项目背景与规范定名
* **项目名称**：**谷歌正重力 (Antigravity 2.0 桌面端深度汉化工程)**
* **作者**：**@LKC218**
* **起点版本**：**v2.0 (统一演进基准)**
* **项目根目录**：`G:\项目\反重力汉化`
* **应用类型**：Electron 桌面端 (Node.js + Chromium + React SPA + Go Language Server)
* **核心原则**：
  1. **零外部环境依赖**：纯原生 Python 二进制 ASAR 引擎与单文件独立 EXE 分发包，普通机器免装 Node/npm/Python；
  2. **高可用可逆守护**：官方版本热更新毫秒级自动感知修复，支持无损一键还原官方纯英文；
  3. **外置词库协同共建**：核心字典外置为 `i18n.js`，任何人用记事本即可免编译扩充词条；
  4. **现代极简交互**：520×430 紧凑界面，主操作一目了然，高级设置抽屉收纳。

---

## 二、 关键架构与核心文件分布

| 文件/目录 | 作用说明 | 运行时/工程位置 |
| :--- | :--- | :--- |
| **`i18n.js`** | 核心汉化字典、DOM 动态监听与注入引擎 (包含精准词典、动态正则、Scope 上下文监听) | `C:\Users\Administrator\.gemini\antigravity\i18n.js`<br>*(本工程根目录下保持同步)* |
| **`tool/`** | 纯 Python 零依赖 GUI 汉化助手源代码工程 (纯原生 ASAR 解析打包、现代化 Tkinter 界面) | `tool/` 目录 (可直接 `python tool/main.py`) |
| **`release/`** | 开箱即用绿色独立免安装分发包 (包含 37MB 单文件 EXE、外置词库、CHANGELOG、素材库) | `release/谷歌正重力_汉化分发包/` |
| **`CHANGELOG.md`** | 规范版本演变记录文档 (以 @LKC218、v2.0 为基准起点) | `CHANGELOG.md` |
| **`README.md`** | 项目整体说明与两种使用方式文档 | `README.md` |
| **`docs/`** | 架构实施方案与交付验收文档 | `docs/实施方案.md`<br>`docs/交付与使用指南.md` |

---

## 三、 当前系统已达成状态 (100% 达成)

1. **顶栏品牌与原生图标**：
   - 窗口标题与顶栏：专属萌态破损趴地 A 彩色图标 + **`谷歌正重力`**；
   - 菜单栏：**`文件`**、**`视图`**、**`窗口`** 及其全部二级下拉菜单 100% 汉化。
2. **侧边栏与主工作区**：
   - `新建会话`、`参考历史会话知识`、`计划任务`、`项目管理` 等核心导航与提示全量中文化。
3. **独立单文件绿色分发包**：
   - 生成 `release/谷歌正重力_汉化分发包/谷歌正重力汉化助手.exe`；
   - 深度注入 PE 文件属性（作者: `@LKC218`，版本: `v2.0`）；
   - UI 界面完成现代化降噪重构（520×430 紧凑尺寸，大号一键安装按钮，次级并排操作，抽屉式高级设置）。

---

## 四、 下一步核心待办任务（新会话执行目标）

在新会话中，我们将重点落地两个全新体验特性：

### 任务 1：思考过程框架汉化 + 动态趣味幽默金句轮播池
- **防卡死铁律**：定向定位状态条容器，节点打上 `data-agy-humor="true"` 属性防重复处理；思考期间以 5 秒步长轻量轮播，一旦进入正文回复生成，**定时器立刻销毁**。
- **框架摘要与动作谓词汉化**：
  将 `Explored X files, Y searches, ran Z commands` 动态替换为 `已探索 X 个文件、Y 次搜索，运行了 Z 条命令`；`Ran ...` 替换为 `已执行 ...`；`Searched ...` 替换为 `已检索 ...`；`Thought for Xs` 替换为 `思考耗时 X 秒`。
- **趣味幽默金句轮播**：
  在 Agent 处于思考/生成/执行时，像 WorkBuddy 一样在状态条上动态展现趣味金句（如 `[正在认真想，毕竟糊弄你良心会痛]`、`[脑细胞正在以 120 迈速度狂奔]`），并在 `i18n.js` 中外置金句池供用户自由增删。

### 任务 2：任务结束时自动挂载 Token / 算力额度消耗统计徽章
- **防卡死铁律（核心）**：
  1. **流式期间绝对静默**：模型在打字输出正文时，统计引擎 100% 休眠，禁止在流式过程中频繁统计；
  2. **事后一次性结算 (One-Shot)**：仅在检测到整轮生成彻底结束时，单次执行轻量分词估算（`< 0.5ms`）；
  3. **禁止重型依赖**：原生纯 JavaScript 字符加权计算，杜绝加载 WASM / Python 等大内存分词器。
- **前端即时分词估算**：
  在每轮模型回复彻底结束时，计算 Prompt + Thinking + Reply 的 Token 规模；
- **界面挂载**：
  在回复气泡底部自动挂载优雅胶囊徽章：
  `共消耗 ✧ 12.8` 或 `📊 本轮消耗: 约 1,280 Tokens · 耗时 16s`。

---

## 五、 在新会话中开启对话的推荐提示词 (复制即用)

在新会话中，直接发送以下指令即可立即承接并开工：

```text
你好！我们正在进行“谷歌正重力”汉化工程。请先阅读项目根目录下的 CONTEXT.md、README.md、CHANGELOG.md 以及 docs/实施方案.md，了解当前 v2.0 架构、已部署的“五重防卡死保险体系”与最新进展。

然后严格遵循 CONTEXT.md 中的“防卡死铁律（Zero-Freeze Guardrails）”，为我执行第四部分的下一步核心任务：
1. 思考过程框架汉化与动态趣味幽默金句轮播（对标 WorkBuddy 趣味状态，随用随销定时器）；
2. 任务结束时自动统计并展示 Token / 算力额度消耗徽章（对标 WorkBuddy 共消耗 ✧ XX，仅在生成结束单次结算，严禁流式中计算）。

请按规划安全有序开展实施，并在完成后热重载验证！
```
